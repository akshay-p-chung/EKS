apiVersion: apps/v1
kind: Deployment
metadata:
  name: gnpt-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: gnpt-service
  template:
    metadata:
      labels:
        app: gnpt-service
    spec:
      containers:
      - name: gnpt-service
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        ports:
        - containerPort: 80
